# Dependencias de desarrollo/CI del backend (no se instalan en la imagen Docker).
# TestClient de FastAPI requiere httpx; pip-audit corre en CI (SEC-14).
pytest>=9.1.1
httpx>=0.28.1
pip-audit>=2.10.1
