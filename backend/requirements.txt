fastapi>=0.141.1,<1.0
uvicorn[standard]>=0.52.4,<1.0
sqlalchemy>=2.0.52,<3.0
psycopg2-binary>=2.9.12,<3.0
python-dotenv>=1.2.3,<2.0
# Cliente del IAM. Se prefiere a urllib (stdlib) porque urllib solo admite UN timeout
# global y acá hace falta separar connect de read: un IAM colgado no puede quedarse
# con un hilo del threadpool. Además es la dependencia que ya usa COIPO_APPTEST.
httpx>=0.28.1,<1.0
# Firma de la cookie de state y de sesión (patrón BFF). Un módulo, cero dependencias,
# mantenido por Pallets. Se descartó starlette.SessionMiddleware porque solo admite un
# TTL absoluto, y acá el esquema deslizante + absoluto + jitter es justo el knob del
# que depende el requisito de las 8:00.
itsdangerous>=2.2,<3.0
